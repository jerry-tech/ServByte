# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB
# All files uploaded through the REST API will be stored in this directory
mealfile.upload-dir=src/main/resources/static
restlogo.upload-dir=src/main/resources/static
deliveryfile.upload-dir=src/main/resources/static
# app version
app.version = 1.0.0
spring.jackson.serialization.fail-on-empty-beans=false
spring.jpa.show-sql=true


spring.mail.host= smtp.gmail.com
spring.mail.port= 587
spring.mail.username= servbyteservices@gmail.com
spring.mail.password= kekoxwavfodcbkcm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

#setting that the json object should not return any null value
spring.jackson.default-property-inclusion= NON_NULL